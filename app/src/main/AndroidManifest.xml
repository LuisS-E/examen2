<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="luis.itics.tesoem.edu.examen">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".salida"></activity>
        <activity android:name=".potencia" />
        <activity android:name=".resta" />
        <activity android:name=".Resta" />
        <activity android:name=".Suma" />
        <activity android:name=".Operaciones" />
        <activity android:name=".Saludo" />
        <activity android:name=".pantalla2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>